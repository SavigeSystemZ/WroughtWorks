# `app/src/` — Application source

Primary source code for **this app**.

- **What goes here:** all runtime/product source. Organize by the
  architecture the chosen stack calls for (the meta-system will help pick
  one once `PRODUCT_BRIEF.md` is filled).
- **When to use:** as soon as the app is defined and you start building
  features. Replace this README's role with real entrypoints/modules.
- **How to build it out:** drive structure from the product brief and the
  `_system/` planning/validation gates — not from guesswork. Keep it
  independent from `_system/` and `bootstrap/`.
- **Empty state:** the `.gitkeep` here means no app source exists yet — the
  app is still undefined or unstarted.
