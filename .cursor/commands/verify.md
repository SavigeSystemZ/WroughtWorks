Run the relevant validation commands from `_system/PROJECT_PROFILE.md` and `_system/VALIDATION_GATES.md`.

Report:

1. commands run
2. pass/fail status
3. scope covered by each command
4. remaining risk
5. whether handoff files need updates
