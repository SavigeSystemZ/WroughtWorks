# External Agent Surface Harvest Protocol

`bootstrap/harvest-agent-surfaces.sh` operates read-only by default and collects
sanitized reusable patterns from approved source trees.

Never copy:
- secrets
- app-specific private facts
- tokens or credentials

Always record donor provenance and sanitization notes.
