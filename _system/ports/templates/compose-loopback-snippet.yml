# Example only: bind published port to loopback on the host.
# Replace HOST_PORT and CONTAINER_PORT after governed allocation.
services:
  app:
    ports:
      - "127.0.0.1:${HOST_PORT}:${CONTAINER_PORT}"
