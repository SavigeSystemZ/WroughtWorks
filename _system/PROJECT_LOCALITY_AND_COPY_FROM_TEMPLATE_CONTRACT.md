# Project Locality And Copy-From-Template Contract

The parent template is scaffold source. Downstream project repos are execution
authority after scaffold.

## Rules

- Improve parent template only in parent-template mode.
- Use project-local copies for downstream app work.
- Do not write outside project root without explicit approval.

