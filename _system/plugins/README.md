# Plugins

Use this directory for domain-specific AIAST extensions that should remain optional.

Each plugin should declare:

- what hook points it uses
- which files it owns
- which bootstrap or validation commands it adds
- which version of AIAST it expects
